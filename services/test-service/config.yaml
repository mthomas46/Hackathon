service_name: test-service
environment: development

server:
  host: 127.0.0.1
  port: 9000
  debug: true

redis:
  host: localhost
  port: 6379
  db: 0

logging:
  level: INFO
  console: true

security:
  jwt_secret: test-secret-key-for-development-only
