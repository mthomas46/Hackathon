server:
  port: 5095
notification:
  owner_map_json: ${NOTIFY_OWNER_MAP_JSON:-}
  owner_map_file: ${NOTIFY_OWNER_MAP_FILE:-}
  deduplication_window_seconds: ${NOTIFICATION_DEDUPE_WINDOW:-300}
  max_retries: ${NOTIFICATION_MAX_RETRIES:-3}
  cache_ttl_seconds: ${NOTIFICATION_CACHE_TTL:-3600}
security:
  enable_ssl: ${SSL_ENABLED:-false}
  ssl_cert_path: ${SSL_CERT_PATH:-}
  ssl_key_path: ${SSL_KEY_PATH:-}
  cors_origins: ${CORS_ORIGINS:-http://localhost:3000}
  rate_limiting_enabled: ${RATE_LIMITING_ENABLED:-true}
  max_request_size: ${MAX_REQUEST_SIZE:-10485760}
  timeout_seconds: ${TIMEOUT_SECONDS:-30}
  enable_request_logging: ${REQUEST_LOGGING:-false}
  enable_metrics: ${METRICS_ENABLED:-true}
development:
  debug_mode: ${DEBUG_MODE:-false}
  enable_cors: ${ENABLE_CORS:-true}
  mock_external_services: ${MOCK_EXTERNAL:-false}
  log_level: ${LOG_LEVEL:-INFO}
