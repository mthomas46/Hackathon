# Production dependencies for discovery-agent service
# Last Updated: October 9, 2025
# Service: discovery-agent v2.0.0
# Architecture: DDD + Clean Architecture
# Quality: A+

# ============================================================================
# Core Web Framework
# ============================================================================
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# ============================================================================
# HTTP Client (Async)
# ============================================================================
httpx>=0.27.0

# ============================================================================
# Configuration
# ============================================================================
pyyaml>=6.0.1
python-dotenv>=1.0.0

# ============================================================================
# Service Dependencies
# ============================================================================
# Note: discovery-agent has minimal dependencies by design
# It discovers other services but doesn't require many external libraries

# No ML/AI libraries needed (tool generation is deterministic)
# No database needed (stateless service)
# No Redis needed (no caching required)
# No heavy data processing needed

# ============================================================================
# Optional Production Dependencies
# ============================================================================
# Uncomment if needed:
# structlog>=24.1.0        # Structured logging
# prometheus-client>=0.20.0 # Metrics (if using Prometheus)
# sentry-sdk>=1.40.0       # Error tracking (if using Sentry)
