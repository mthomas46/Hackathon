# Requirements for validation and hardening scripts
# These scripts need additional dependencies beyond the shared requirements

# HTTP client for health monitoring
requests>=2.32.0
aiohttp>=3.12.0

# YAML parsing (already in shared, but explicit for scripts)
pyyaml>=6.0.0

# Redis client (already in shared, but explicit for scripts)
redis>=6.4.0

# Additional utilities for advanced validation
docker>=7.0.0
kubernetes>=26.0.0

# Testing and validation frameworks
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# CLI and terminal UI
click>=8.0.0
rich>=13.0.0

# Data processing and analysis
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0

# Monitoring and alerting
prometheus-client>=0.20.0
grafana-api>=1.0.0

# Configuration management
python-dotenv>=1.0.0

# Logging and error handling
structlog>=23.0.0
sentry-sdk>=1.0.0

# Network and system utilities
netifaces>=0.11.0
psutil>=5.9.0

# Database clients
sqlalchemy>=2.0.0
pymongo>=4.0.0

# API documentation
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0

# Code quality
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
